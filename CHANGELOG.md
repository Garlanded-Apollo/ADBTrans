# Changelog

## v1.0.8

### New Features
- 拖拽文件到桌面/文件夹实现快捷下载（非图片文件使用对应类型图标）
- 支持多文件拖拽下载（勾选多个文件后拖拽，与下载按钮逻辑一致）
- 拖拽下载任务体现在传输队列中，可查看进度
- ADB push 遇到只读文件系统时自动尝试 `adb root` + `adb remount` 后重试

### Improvements
- 标题栏右上角移除冗余的设备序列号显示

## v1.0.7

### Bug Fixes
- 修复历史记录重复问题，基于完整路径规范化去重
- 历史记录按访问时间倒序排列（最近访问排最前）
- 移除历史记录的持续高亮，仅保留 hover 效果
- 启动时自动清除旧的重复缓存数据

## v1.0.6

### New Features
- 左侧边栏宽度可拖拽调节（180px ~ 400px）
- 右侧文件预览区域支持收起/展开，默认收起，工具栏点击图标切换
- 上传按钮合并为下拉菜单，支持选择上传文件或文件夹

### Bug Fixes
- 历史记录去重逻辑修复，避免同路径多条记录同时高亮
- 历史记录标签显示修复，`/storage/emulated/0` 正确显示为 `sdcard`
- 历史记录需停留 2 秒以上才写入，避免快速浏览产生大量记录
- 路径栏显示 `/sdcard/` 前缀而非 `/storage/emulated/0/`

### Improvements
- 移除工具栏删除按钮
- 文件列表列宽调整触发区域从 1px 扩大到 8px，更易拖拽
- 路径输入框增加背景色，视觉更醒目
- 传输进度优化：单文件按大小计算百分比，文件夹按文件个数计算百分比
- 文件夹传输速度显示为「文件/s」

## v1.0.4

### New Features
- 关闭窗口时最小化到系统托盘，而非直接退出
- 托盘右键菜单支持「打开」和「退出」操作
- macOS 托盘图标采用模板风格，自动适配深色/浅色模式

### Bug Fixes
- macOS 点击 Dock 图标可恢复已隐藏的窗口
- 托盘右键「退出」可彻底关闭应用（不再被 close 事件拦截）

### Improvements
- 添加单实例锁，防止多开窗口
- 文件传输进度检测从同步改为异步，避免阻塞主线程导致窗口拖动卡顿
- 传输进度轮询间隔从 500ms 调整为 1000ms，降低 IPC 开销
